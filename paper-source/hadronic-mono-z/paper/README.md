# HTMHT Paper Editor

Edit `v1.tex`.

Build once:

```powershell
.\build.cmd
```

Build and monitor for changes:

```powershell
.\watch.cmd
```

The PowerShell scripts provide the same commands for shells that allow
local script execution.
